# RamaLama Troubleshooting Guide

Руководство по решению проблем при работе с RamaLama через Docker.

## 📋 Содержание

- [Проблемы с прокси](#проблемы-с-прокси)
- [Проблемы с Docker](#проблемы-с-docker)
- [Проблемы с моделями](#проблемы-с-моделями)
- [Проблемы с производительностью](#проблемы-с-производительностью)
- [Частые ошибки](#частые-ошибки)
- [Логирование и отладка](#логирование-и-отладка)

---

## 🌐 Проблемы с прокси

### Проблема: Зацикливание на логах `INFO:ramalama:Using proxy`

**Симптомы:**
```
INFO:ramalama:Using proxy: ftp=http://127.0.0.1:2080/
INFO:ramalama:Using proxy: https=http://127.0.0.1:2080/
INFO:ramalama:Using proxy: http=http://127.0.0.1:2080/
INFO:ramalama:Using proxy: all=socks://127.0.0.1:2080/
[повторяется бесконечно...]
```

**Решение:**

✅ **Уже исправлено в нашей конфигурации!**

Проверьте, что используется наша версия:
```bash
# В entrypoint.sh должно быть:
export RAMALAMA_LOG_LEVEL="ERROR"
```

Если проблема осталась:
```bash
# Пересоберите образ
./ramalama.sh rebuild
```

---

### Проблема: Прокси недоступен из контейнера

**Симптомы:**
```
Connection refused
Unable to connect to proxy
```

**Диагностика:**
```bash
# 1. Проверьте прокси на хосте
curl -I --proxy http://127.0.0.1:2080 https://google.com

# 2. Проверьте из контейнера
./ramalama.sh shell
curl -I https://google.com
```

**Решения:**

1. **Используйте host network** (уже настроено в docker-compose.yml):
```yaml
network_mode: host
```

2. **Проверьте, что прокси запущен:**
```bash
netstat -tuln | grep 2080
# или
lsof -i :2080
```

3. **Если прокси на другом хосте**, измените в `.env`:
```bash
HTTP_PROXY=http://192.168.1.100:2080
```

---

### Проблема: Работа без прокси

**Если прокси не нужен:**

```bash
# Вариант 1: Используйте профиль без прокси
docker-compose --profile no-proxy run ramalama-no-proxy list

# Вариант 2: Закомментируйте в docker-compose.yml
environment:
  # - HTTP_PROXY=http://127.0.0.1:2080
  # - HTTPS_PROXY=http://127.0.0.1:2080
```

---

## 🐳 Проблемы с Docker

### Проблема: Docker образ не собирается

**Симптомы:**
```
ERROR: failed to solve
```

**Решения:**

1. **Очистите кэш и пересоберите:**
```bash
./ramalama.sh rebuild
```

2. **Проверьте место на диске:**
```bash
df -h
docker system df
```

3. **Очистите Docker:**
```bash
docker system prune -a
```

---

### Проблема: Контейнер не запускается

**Диагностика:**
```bash
# Проверьте логи
docker-compose logs

# Проверьте статус
docker ps -a

# Подробная информация
docker inspect ramalama
```

**Решения:**

1. **Проверьте права доступа:**
```bash
ls -la models/ data/
chmod -R 755 models/ data/
```

2. **Проверьте entrypoint:**
```bash
chmod +x entrypoint.sh
```

3. **Пересоздайте контейнер:**
```bash
docker-compose down -v
docker-compose up
```

---

### Проблема: Volume не монтируется

**Симптомы:**
- Модели не сохраняются после перезапуска
- Изменения в `models/` не видны в контейнере

**Решения:**

1. **Проверьте пути в docker-compose.yml:**
```yaml
volumes:
  - ./models:/workspace/models  # Относительный путь
  # или
  - /absolute/path/models:/workspace/models  # Абсолютный путь
```

2. **Создайте директории вручную:**
```bash
mkdir -p models data
```

3. **Проверьте SELinux (если используется):**
```bash
chcon -Rt svirt_sandbox_file_t models/ data/
```

---

## 🤖 Проблемы с моделями

### Проблема: Модель не скачивается

**Симптомы:**
```
Error downloading model
Connection timeout
```

**Решения:**

1. **Проверьте прокси:**
```bash
./ramalama.sh shell
curl -I https://huggingface.co
```

2. **Проверьте место на диске:**
```bash
df -h ./models
```

3. **Попробуйте другую модель:**
```bash
# Маленькая модель для теста
./ramalama.sh pull tinyllama
```

4. **Увеличьте таймауты:**
```bash
# В docker-compose.yml добавьте
environment:
  - RAMALAMA_DOWNLOAD_TIMEOUT=600
```

---

### Проблема: Модель скачалась, но не запускается

**Диагностика:**
```bash
# Проверьте список моделей
./ramalama.sh list

# Проверьте файлы
ls -lh models/

# Проверьте целостность
./ramalama.sh -- verify <model_name>
```

**Решения:**

1. **Пересчайте модель:**
```bash
./ramalama.sh rm <model_name>
./ramalama.sh pull <model_name>
```

2. **Проверьте память:**
```bash
free -h
# Модели требуют RAM минимум в 2x от размера
```

---

### Проблема: Медленная загрузка моделей

**Решения:**

1. **Используйте зеркала Hugging Face:**
```bash
export HF_ENDPOINT=https://hf-mirror.com
```

2. **Скачайте через браузер и поместите в models/:**
```bash
# Скачать вручную, затем:
cp ~/Downloads/model.gguf models/
./ramalama.sh list  # Должна появиться
```

3. **Используйте resume для больших моделей:**
```bash
# RamaLama автоматически возобновляет загрузку
```

---

## ⚡ Проблемы с производительностью

### Проблема: Медленная генерация текста

**Решения:**

1. **Используйте меньшую модель:**
```bash
# Вместо 7B используйте 1B
./ramalama.sh pull llama3.2:1b
```

2. **Ограничьте контекст:**
```bash
./ramalama.sh run <model> --context-size 2048
```

3. **Настройте batch size:**
```bash
./ramalama.sh run <model> --batch-size 512
```

4. **Выделите больше памяти Docker:**
```yaml
# docker-compose.yml
deploy:
  resources:
    limits:
      memory: 8G
```

---

### Проблема: Высокое использование CPU

**Решения:**

1. **Используйте GPU (если доступен):**
```yaml
# docker-compose.yml
deploy:
  resources:
    reservations:
      devices:
        - driver: nvidia
          count: 1
          capabilities: [gpu]
```

2. **Ограничьте количество потоков:**
```bash
./ramalama.sh run <model> --threads 4
```

---

### Проблема: Out of Memory

**Симптомы:**
```
Out of memory
Killed
137 exit code
```

**Решения:**

1. **Используйте меньшую модель:**
```bash
# Примерные требования:
# 1B модель ~2GB RAM
# 3B модель ~4GB RAM
# 7B модель ~8GB RAM
```

2. **Увеличьте swap:**
```bash
sudo dd if=/dev/zero of=/swapfile bs=1G count=8
sudo mkswap /swapfile
sudo swapon /swapfile
```

3. **Используйте quantized модели:**
```bash
# Q4 или Q5 вместо Q8
./ramalama.sh pull llama3.2:1b-q4
```

---

## ❌ Частые ошибки

### `Permission denied`

**Решение:**
```bash
chmod +x ramalama.sh entrypoint.sh quick-test.sh
chmod -R 755 models/ data/
```

---

### `Port already in use`

**Решение:**
```bash
# Найти процесс
lsof -i :8080

# Остановить
kill <PID>

# Или использовать другой порт
./ramalama.sh serve <model> --port 8081
```

---

### `Image not found`

**Решение:**
```bash
./ramalama.sh build
```

---

### `Cannot connect to Docker daemon`

**Решение:**
```bash
# Запустите Docker
sudo systemctl start docker

# Добавьте пользователя в группу
sudo usermod -aG docker $USER
newgrp docker
```

---

## 🔍 Логирование и отладка

### Включить подробные логи

```bash
# Вариант 1: Изменить в docker-compose.yml
environment:
  - RAMALAMA_LOG_LEVEL=DEBUG

# Вариант 2: Временно
docker-compose run -e RAMALAMA_LOG_LEVEL=DEBUG ramalama <command>
```

---

### Посмотреть логи контейнера

```bash
# Все логи
docker-compose logs

# Последние 50 строк
docker-compose logs --tail=50

# В реальном времени
docker-compose logs -f

# Логи конкретного контейнера
docker logs ramalama
```

---

### Отладка внутри контейнера

```bash
# Открыть shell
./ramalama.sh shell

# Внутри контейнера:
ramalama --version
ramalama info
env | grep -i proxy
curl -I https://google.com
```

---

### Сохранить логи для анализа

```bash
# Логи Docker
docker-compose logs > docker-logs.txt

# Системная информация
./ramalama.sh info > system-info.txt

# Список моделей
./ramalama.sh list > models-list.txt

# Отправьте эти файлы при обращении за помощью
```

---

## 🆘 Экстренное восстановление

### Полная переустановка

```bash
# 1. Остановить и удалить всё
./ramalama.sh clean
docker system prune -a

# 2. Сохранить модели (опционально)
./backup.sh create

# 3. Переустановить
./ramalama.sh build
./quick-test.sh

# 4. Восстановить модели (если нужно)
./backup.sh restore backups/ramalama_backup_*.tar.gz
```

---

### Сброс к заводским настройкам

```bash
# ВНИМАНИЕ: Удалит все модели и данные!

rm -rf models/* data/*
./ramalama.sh rebuild
./ramalama.sh info
```

---

## 📞 Получить помощь

1. **Проверьте логи:**
```bash
./monitor.sh -s > debug-info.txt
```

2. **Запустите диагностику:**
```bash
./quick-test.sh > test-results.txt
```

3. **Соберите информацию:**
```bash
echo "=== System Info ===" > support-info.txt
uname -a >> support-info.txt
docker --version >> support-info.txt
docker-compose --version >> support-info.txt
echo "=== Environment ===" >> support-info.txt
env | grep -i proxy >> support-info.txt
```

4. **Проверьте документацию:**
- README.md - основная документация
- examples.sh - примеры использования
- Официальная документация RamaLama

---

## ✅ Чеклист проверки

Перед обращением за помощью проверьте:

- [ ] Docker запущен и работает
- [ ] Образ собран (`docker images | grep ramalama`)
- [ ] Достаточно места на диске (`df -h`)
- [ ] Права доступа правильные (`ls -la models/`)
- [ ] Прокси настроен корректно (если используется)
- [ ] Скрипты исполняемые (`ls -l *.sh`)
- [ ] Логи просмотрены (`docker-compose logs`)
- [ ] Тесты пройдены (`./quick-test.sh`)

---

**Последнее обновление:** 2024

---

## 🌐 Translations / Переводы

| Язык | Документация | Статус перевода |
|------|--------------|-----------------|
| 🇺🇸 English | [TROUBLESHOOTING.en.md](../docs/TROUBLESHOOTING.en.md) | ✅ Translation |
| 🇷🇺 Russian | [TROUBLESHOOTING.md](TROUBLESHOOTING.md) | ✅ Оригинал |
| 🇺🇸 English | [README.md](../README.md) | ✅ Translation |
| 🇷🇺 Russian | [README.ru.md](../logs/README.ru.md) | ✅ Оригинал |
