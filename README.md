# RamaLama Docker Project

🚀 Полнофункциональное решение для запуска RamaLama через Docker с поддержкой proxy, мониторингом и автоматизацией.

## 🎯 Особенности

- ✅ **Полная поддержка HTTP/HTTPS proxy** - работает из коробки
- ✅ **Решена проблема зацикливания прокси** - нет бесконечных логов
- ✅ **Автоматическая установка** - один скрипт для настройки всего
- ✅ **Улучшенная диагностика** - подробная информация о системе и моделях
- ✅ **Автоматическое исправление проблем** - инструменты диагностики и восстановления
- ✅ **Команды Make** - быстрый доступ к всем операциям
- ✅ **Продвинутое логирование** - подробные логи для отладки
- ✅ **Автоматическая ротация логов** - предотвращение переполнения диска
- ✅ **Гибкое управление** - скрипты для всех операций
- ✅ **Мониторинг в реальном времени** - отслеживание состояния системы
- ✅ **Система бэкапов** - безопасное хранение моделей
- ✅ **Примеры использования** - готовые сценарии работы
- ✅ **Подробная документация** - решение всех проблем

## 📁 Структура проекта

```
ramalama-project/
├── Dockerfile                 # Определение Docker образа
├── docker-compose.yml        # Конфигурация Docker Compose
├── entrypoint.sh             # 🆕 Улучшенная точка входа с диагностикой
├── install.sh                # 🆕 Автоматическая установка
├── ramalama.sh               # 🆕 Главный скрипт управления (исправления)
├── quick-test.sh             # Быстрое тестирование
├── examples.sh               # 🆕 Примеры использования
├── monitor.sh                # 🆕 Мониторинг системы
├── backup.sh                 # 🆕 Бэкапы моделей
├── Makefile                  # 🆕 Команды Make (расширенный)
├── debug-download.sh         # 🆕 Диагностика проблем скачивания
├── setup-logrotate.sh        # 🆕 Настройка автоматической ротации логов
├── .env.example              # 🆕 Пример конфигурации
├── .gitignore                # Git игнорирование
├── README.md                 # Эта документация
├── LOGROTATION_GUIDE.md      # 🆕 Руководство по ротации логов
├── TROUBLESHOOTING.md        # 🆕 Решение проблем
├── models/                   # Хранилище моделей (авто)
├── logs/                     # Логи системы (авто)
│   └── sessions/             # Сессионные логи (авто)
├── cache/                    # Кэш для ускорения работы (авто)
├── data/                     # Данные (авто)
│   └── logs/                 # Логи (авто)
└── backups/                  # Бэкапы (авто)
```

## 🚀 Быстрый старт

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# Один скрипт для всего!
chmod +x install.sh
./install.sh

# Установка автоматически:
# ✓ Проверит системные требования
# ✓ Настроит прокси (если нужен)
# ✓ Создаст конфигурацию
# ✓ Соберет Docker образ
# ✓ Запустит тесты
```

### Вариант 2: Ручная установка

```bash
# 1. Сделать скрипты исполняемыми
chmod +x *.sh

# 2. Собрать образ
./ramalama.sh build

# 3. Запустить тесты
./quick-test.sh

# 4. Проверить работу
./ramalama.sh version
./ramalama.sh info
```

## 🛠️ Дополнительные утилиты

### Мониторинг системы

```bash
# Интерактивный мониторинг
./monitor.sh

# Одноразовый снимок
./monitor.sh --snapshot

# JSON вывод для автоматизации
./monitor.sh --json

# С кастомным интервалом
./monitor.sh --interval 10
```

### Бэкапы

```bash
# Создать бэкап
./backup.sh create

# Список бэкапов
./backup.sh list

# Восстановить из бэкапа
./backup.sh restore backups/ramalama_backup_*.tar.gz

# Очистить старые бэкапы (оставить последние 3)
./backup.sh cleanup 3
```

### Примеры использования

```bash
# Показать все примеры
./examples.sh

# Примеры выполняют демонстрацию:
# - Базовых операций
# - Работы с моделями
# - Интерактивного чата
# - Режима сервера
# - Автоматизации
```

### 🩺 Диагностические инструменты

#### Автоматическая диагностика проблем скачивания

```bash
# Полная диагностика проблем с моделями
./debug-download.sh

# Проверяет:
# - Доступность Hugging Face
# - Настройки прокси
# - Права доступа к директориям
# - Состояние Docker volumes
# - Сетевые подключения
```

#### Автоматическое исправление Docker volumes

#### Проверка монтирования директорий

```bash
# Проверка монтирования через профиль
docker-compose --profile check run --rm check-mounts

# Или используйте простую проверку:
ls -la models/ data/
/ data/
```

### 🆕df -h models Система логирования

#### Автоматическое сохранение логов

Логи RamaLama теперь автоматически сохраняются в файлы:

```bash
# Инициализация системы логирования
./log-manager.sh init

# Просмотр последних логов
./ramalama.sh logs show

# Мониторинг логов в реальном времени
./ramalama.sh logs tail

# Очистка старых логов
./ramalama.sh logs clean

# Статус системы логирования
./ramalama.sh logs status
```

#### 📋 Автоматическая ротация логов

Система включает автоматическую ротацию логов для предотвращения переполнения диска:

- **Ежедневная ротация** всех логов
- **Хранение 30 архивных копий** с автоматическим сжатием
- **Настройка через logrotate** и cron
- **Подробная инструкция**: [`LOGROTATION_GUIDE.md`](LOGROTATION_GUIDE.md)

```bash
# Быстрая настройка автоматической ротации
sudo ./setup-logrotate.sh

# Ручная очистка старых логов
./log-manager.sh clean

# Принудительная ротация
logrotate -f config/logrotate-ramalama.conf
```

#### Файлы логов

- **Основной файл**: `./logs/ramalama.log`
- **Сессионные логи**: `./logs/sessions/ramalama_session_*.log`
- **Логи мониторинга**: `./logs/monitor.log`

#### Команды с автоматическим логированием

```bash
# Эти команды автоматически сохраняют логи:
./ramalama.sh pull <model>      # Скачивание с логированием
./ramalama.sh run <model>       # Запуск с логированием
./ramalama.sh serve <model>     # Сервер с логированием
```

### 📋 Команды Make (обновлено)

```bash
# Показать все доступные команды
make help

# Основные команды:
make setup-dirs    # Проверить и создать директории
make build         # Собрать Docker образ
make rebuild       # Пересобрать образ с нуля
make test          # Запустить тесты
make clean         # Очистить контейнеры и образы

# Информация:
make info          # Показать информацию о системе
make list          # Список локальных моделей
make version       # Показать версию ramalama

# Использование моделей:
make pull MODEL=tinyllama     # Скачать модель
make run MODEL=tinyllama      # Запустить модель
make serve MODEL=tinyllama PORT=8080  # Запустить сервер

# Администрирование:
make backup        # Создать бэкап моделей
make monitor       # Запустить мониторинг системы

# Установка:
make install       # Полная настройка проекта
```

## 📖 Использование

### Управление моделями

```bash
# Скачать модель
./ramalama.sh pull llama3.2:1b

# Список скачанных моделей
./ramalama.sh list

# Удалить модель
./ramalama.sh rm llama3.2:1b
```

### Запуск моделей

```bash
# Интерактивный режим
./ramalama.sh run llama3.2:1b

# Запуск как сервер
./ramalama.sh serve llama3.2:1b --port 8080

# С дополнительными параметрами
./ramalama.sh run llama3.2:1b --temperature 0.7
```

### Дополнительные команды

```bash
# Открыть bash в контейнере
./ramalama.sh shell

# Пересобрать образ с нуля
./ramalama.sh rebuild

# Очистить контейнеры и образы
./ramalama.sh clean

# Прямой доступ к ramalama
./ramalama.sh -- <любая команда>
```

## ⚙️ Настройка конфигурации

### Файл .env - Централизованная конфигурация

Все настройки проекта управляются через файл `.env`, который автоматически загружается Docker Compose.

#### Создание и настройка .env файла

```bash
# 1. Скопируйте пример конфигурации
cp .env.example ./config/.env

# 2. Отредактируйте под свои нужды
nano .env
```

#### Структура .env файла

```bash
# ============================================
# НАСТРОЙКИ ПРОКСИ
# ============================================

# HTTP/HTTPS прокси (если используется)
HTTP_PROXY=http://127.0.0.1:2080
HTTPS_PROXY=http://127.0.0.1:2080

# Исключения для прокси
NO_PROXY=localhost,127.0.0.0/8,::1,host.docker.internal

# ============================================
# НАСТРОЙКИ RAMALAMA
# ============================================

# Путь к моделям внутри контейнера
RAMALAMA_MODELS_PATH=/workspace/models

# Уровень логирования: DEBUG, INFO, WARNING, ERROR
RAMALAMA_LOG_LEVEL=ERROR

# Директория для данных
RAMALAMA_DATA_PATH=/workspace/data

# ============================================
# НАСТРОЙКИ DOCKER
# ============================================

# Имя образа
IMAGE_NAME=ramalama
IMAGE_TAG=latest

# Имя контейнера
CONTAINER_NAME=ramalama

# ============================================
# НАСТРОЙКИ МОДЕЛЕЙ
# ============================================

# Модель по умолчанию для быстрого запуска
DEFAULT_MODEL=llama3.2:1b

# Порт для serve режима
DEFAULT_SERVE_PORT=8080
```

#### Сценарии использования .env

**1. Работа с прокси:**
```bash
# В .env файле:
HTTP_PROXY=http://127.0.0.1:2080
HTTPS_PROXY=http://127.0.0.1:2080
```

**2. Работа без прокси:**
```bash
# Закомментируйте или оставьте пустыми:
# HTTP_PROXY=
# HTTPS_PROXY=

# Или используйте профиль без прокси:
docker-compose --profile no-proxy run ramalama-no-proxy <команда>
```

**3. Настройка логирования:**
```bash
# Подробные логи для отладки:
RAMALAMA_LOG_LEVEL=DEBUG

# Минимальные логи в продакшене:
RAMALAMA_LOG_LEVEL=ERROR
```

**4. Смена модели по умолчанию:**
```bash
# В .env файле:
DEFAULT_MODEL=tinyllama
```

#### Автоматическая настройка через install.sh

При запуске `./install.sh` скрипт автоматически:
- Определяет настройки прокси из окружения хоста
- Создает оптимальный .env файл
- Настраивает конфигурацию под вашу систему

## 🔧 Настройка прокси

### Автоматическая настройка (рекомендуется)

Прокси автоматически подхватывается из переменных окружения хоста и записывается в `.env`:

```bash
export HTTP_PROXY=http://127.0.0.1:2080
export HTTPS_PROXY=http://127.0.0.1:2080
```

### Ручная настройка

Отредактируйте секцию "НАСТРОЙКИ ПРОКСИ" в `.env` файле:

```bash
HTTP_PROXY=http://your-proxy:port
HTTPS_PROXY=http://your-proxy:port
NO_PROXY=localhost,127.0.0.0/8,::1
```

### Работа без прокси

```bash
# Вариант 1: Очистите переменные в .env
HTTP_PROXY=
HTTPS_PROXY=

# Вариант 2: Использовать профиль без прокси
docker-compose --profile no-proxy run ramalama-no-proxy <команда>
```

## 🔍 Решение проблем

### Проблема: Зацикливание на логах прокси

**Решение:** Используется упрощенная конфигурация прокси (только HTTP/HTTPS) и отключено избыточное логирование через `RAMALAMA_LOG_LEVEL=ERROR`.

### Проблема: Прокси недоступен из контейнера

**Решение:** Используется `network_mode: host` для прямого доступа к прокси на 127.0.0.1.

### Проблема: Модели не сохраняются

**Решение:** Проверьте примонтированные volumes:

```bash
ls -la models/
```

### Проблема: Медленная загрузка моделей

**Решение:** Проверьте работу прокси:

```bash
# Внутри контейнера
./ramalama.sh shell
curl -I https://huggingface.co
```

### 🆕 Проблемы с Docker volumes и монтированием

**Автоматическое решение:**

```bash
# Исправление всех проблем с volumes
./fix-volumes.sh

# Проверка монтирования директорий
docker-compose --profile check run --rm check-mounts

# Диагностика проблем скачивания
./debug-download.sh
```

**Что проверяют диагностические скрипты:**
- Правильность создания директорий
- Права доступа к файлам
- Состояние Docker volumes
- Сетевые подключения и прокси
- Доступность Hugging Face
- Свободное место на диске

### 🆕 Проблемы с загрузкой моделей

**Автоматическая диагностика:**

```bash
# Полная диагностика проблем скачивания
./debug-download.sh

# Этот скрипт проверит:
# ✓ Доступность Hugging Face API
# ✓ Настройки прокси сервера
# ✓ Права доступа к директории models/
# ✓ Состояние Docker volumes
# ✓ Сетевые подключения
# ✓ Свободное место на диске
```

**Если диагностика выявила проблемы:**

1. **Проблемы с сетью:** Настройте прокси в .env файле
2. **Недостаток места:** Очистите models/ или создайте больше места
3. **Проблемы с прокси:** Проверьте настройки в .env файле

## 📊 Переменные окружения (.env)

Все переменные автоматически загружаются из файла `.env` через Docker Compose. Изменения применяются при перезапуске контейнеров.

### Прокси настройки

| Переменная | Описание | По умолчанию | Пример |
|-----------|----------|--------------|--------|
| `HTTP_PROXY` | HTTP прокси сервер | пусто | `http://127.0.0.1:2080` |
| `HTTPS_PROXY` | HTTPS прокси сервер | пусто | `http://127.0.0.1:2080` |
| `NO_PROXY` | Исключения для прокси | `localhost,127.0.0.0/8,::1` | `localhost,127.0.0.0/8,::1,host.docker.internal` |

### Настройки RamaLama

| Переменная | Описание | По умолчанию | Возможные значения |
|-----------|----------|--------------|-------------------|
| `RAMALAMA_MODELS_PATH` | Путь к моделям | `/workspace/models` | любой путь |
| `RAMALAMA_DATA_PATH` | Путь к данным | `/workspace/data` | любой путь |
| `RAMALAMA_LOG_LEVEL` | Уровень логирования | `ERROR` | `DEBUG`, `INFO`, `WARNING`, `ERROR` |

### Настройки Docker

| Переменная | Описание | По умолчанию | Примечание |
|-----------|----------|--------------|------------|
| `IMAGE_NAME` | Имя Docker образа | `ramalama` | Только для справки |
| `IMAGE_TAG` | Тег Docker образа | `latest` | Только для справки |
| `CONTAINER_NAME` | Имя контейнера | `ramalama` | Только для справка |

### Настройки моделей

| Переменная | Описание | По умолчанию | Примеры |
|-----------|----------|--------------|---------|
| `DEFAULT_MODEL` | Модель по умолчанию | `llama3.2:1b` | `tinyllama`, `phi3:mini`, `llama3.2:3b` |
| `DEFAULT_SERVE_PORT` | Порт для serve режима | `8080` | любой свободный порт |

### Дополнительные настройки (опциональные)

| Переменная | Описание | По умолчанию | Использование |
|-----------|----------|--------------|---------------|
| `MEMORY_LIMIT` | Лимит памяти | не установлен | `8g` - для ограничения использования RAM |
| `CPU_LIMIT` | Лимит CPU | не установлен | `4` - для ограничения ядер |
| `USE_GPU` | Использовать GPU | не установлен | `true` - требует nvidia-docker |

### 🆕 Новые переменные для улучшенного логирования

| Переменная | Описание | По умолчанию | Эффект |
|-----------|----------|--------------|---------|
| `HF_HUB_DISABLE_PROGRESS_BARS` | Отключить прогресс-бары | `false` | Показывает прогресс скачивания |
| `HF_HUB_ENABLE_HF_TRANSFER` | Включить ускорение скачивания | `1` | Ускоряет загрузку моделей |
| `RAMALAMA_SHOW_PROGRESS` | Показывать прогресс операций | `true` | Детальная информация о процессах |

> **Примечание:** Дополнительные настройки закомментированы в .env.example и требуют раскомментирования для использования.

## 🛠️ Продвинутое использование

### Работа с несколькими моделями

```bash
# Скачать несколько моделей
./ramalama.sh pull llama3.2:1b
./ramalama.sh pull phi3:mini

# Запустить разные модели
./ramalama.sh run llama3.2:1b
./ramalama.sh run phi3:mini
```

### Настройка памяти

Отредактируйте `docker-compose.yml`:

```yaml
deploy:
  resources:
    limits:
      memory: 8G
    reservations:
      memory: 4G
```

### Использование GPU

Добавьте в `docker-compose.yml`:

```yaml
deploy:
  resources:
    reservations:
      devices:
        - driver: nvidia
          count: 1
          capabilities: [gpu]
```

## 📝 Примеры работы

### Пример 1: Простой чат

```bash
./ramalama.sh pull llama3.2:1b
./ramalama.sh run llama3.2:1b
# Введите ваш запрос...
```

### Пример 2: API сервер

```bash
./ramalama.sh serve llama3.2:1b --port 8080

# В другом терминале:
curl http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"messages": [{"role": "user", "content": "Hello!"}]}'
```

### Пример 3: Скрипт обработки

```bash
echo "What is the capital of France?" | \
./ramalama.sh run llama3.2:1b --no-interactive
```

## 🔐 Безопасность

- Прокси-креденшиалы хранятся в переменных окружения
- Модели хранятся в изолированном volume
- Контейнер запускается без привилегий

## 📞 Поддержка

- Проверка версии: `./ramalama.sh version`
- Информация о системе: `./ramalama.sh info`
- Справка: `./ramalama.sh help`

## 📚 Документация

### Файлы документации

- **README.md** - Основная документация (этот файл)
- **LOGROTATION_GUIDE.md** - 🆕 Полное руководство по ротации логов
- **TROUBLESHOOTING.md** - Подробное руководство по решению проблем
- **examples.sh** - Практические примеры использования
- **.env.example** - Пример конфигурационного файла

### Встроенная справка

```bash
# Справка по ramalama.sh
./ramalama.sh help

# Справка по monitor.sh
./monitor.sh --help

# Справка по backup.sh
./backup.sh --help

# Команды Make
make help
```

### Получение помощи

Если возникли проблемы:

1. 🩺 **Автоматическая диагностика:**
   ```bash
   ./debug-download.sh    # Диагностика проблем скачивания
   ./fix-volumes.sh       # Исправление проблем с volumes
   ```

2. 🔍 **Проверка монтирования:**
   ```bash
   docker-compose --profile check run --rm check-mounts
   ```

3. 📊 **Мониторинг системы:**
   ```bash
   ./monitor.sh --snapshot  # Полная диагностика
   ./monitor.sh --json      # JSON для автоматизации
   ```

4. 📋 **Проверка логов и тестов:**
   ```bash
   docker-compose logs      # Логи контейнеров
   ./quick-test.sh          # Запуск тестов
   ```

5. 📖 **Документация:**
   - **LOGROTATION_GUIDE.md** - настройка ротации логов
   - **TROUBLESHOOTING.md** - подробные решения проблем
   - **README.md** - эта документация
   - **make help** - список всех команд

## 🎓 Советы и лучшие практики

### Для начинающих

1. Начните с маленькой модели: `./ramalama.sh pull tinyllama`
2. Используйте автоматическую установку: `./install.sh`
3. Изучите примеры: `./examples.sh`
4. Мониторьте систему: `./monitor.sh`

### Для продвинутых

1. **Гибкая конфигурация через .env:**
   - Настройте все параметры через `.env` файл для централизованного управления
   - Используйте разные .env файлы для разных окружений (dev/staging/prod)
   - Пример: `cp .env.example .env.staging && cp .env .env.prod`

2. **Продвинутые настройки прокси:**
   - Настройте NO_PROXY для оптимизации трафика
   - Используйте разные профили для разных сценариев работы
   - Автоматическое определение прокси через install.sh

3. **Оптимизация производительности:**
   - Настройте MEMORY_LIMIT и CPU_LIMIT для контроля ресурсов
   - Включите GPU поддержку через USE_GPU=true
   - Оптимизируйте DEFAULT_MODEL под ваши задачи

4. **Автоматизация и интеграция:**
   - Используйте `make` для быстрого доступа к командам
   - Автоматизируйте бэкапы через cron
   - Используйте мониторинг в JSON для интеграции
   - Настройте webhook'и для уведомлений

### Производительность

1. Используйте квантизированные модели (Q4/Q5) для экономии памяти
2. Ограничьте context-size если не нужен большой контекст
3. Настройте swap для больших моделей
4. Используйте GPU если доступен

## 🤝 Участие в разработке

Проект открыт для улучшений! Присылайте pull requests с:

- Исправлениями багов
- Новыми утилитами
- Улучшениями документации
- Оптимизацией производительности

## 📄 Лицензия

Проект использует RamaLama, который распространяется под лицензией Apache 2.0.

---

**Создано с ❤️ для упрощения работы с RamaLama**

Последнее обновление: Декабрь 2024

## 🔧 Улучшения в Entrypoint

### Новые возможности отображения

Улучшенный `entrypoint.sh` теперь предоставляет:

1. **Подробная информация о директориях:**
   - Автоматическая проверка монтирования volumes
   - Отображение содержимого /workspace/models и /workspace/data
   - Создание директорий если они отсутствуют

2. **Информация о существующих моделях:**
   - Автоматический поиск скачанных моделей (.gguf, .bin файлы)
   - Ограничение вывода до 10 моделей для читаемости

3. **Прогресс скачивания:**
   - Специальные сообщения для команд `pull` и `run`
   - Информация о путях сохранения файлов
   - Индикация отображения прогресса в логах

4. **Улучшенная визуализация:**
   - Эмодзи для лучшего восприятия
   - Структурированный вывод информации
   - Цветовое оформление через каналы stdout/stderr

### Пример вывода

```
🚀 Запуск RamaLama в Docker
📁 Модели сохраняются в: /workspace/models
📁 Данные хранятся в: /workspace/data

=== Проверка монтирования директорий ===
Содержимое /workspace/models:
total 12
drwxr-xr-x 3 root root 4096 Dec 12 07:00 .
drwxr-xr-x 1 root root 4096 Dec 12 07:00 ..
-rw-r--r-- 1 root root 12884901888 Dec 12 06:30 llama3.2-1b-q4_0.gguf

Содержимое /workspace/data:
total 8
drwxr-xr-x 1 root root 4096 Dec 12 07:00 .
drwxr-xr-x 1 root root 4096 Dec 12 07:00 ..

=== Существующие модели ===
/workspace/models/llama3.2-1b-q4_0.gguf

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  RamaLama Docker Environment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Models path: /workspace/models
Proxy: http://127.0.0.1:2080
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

▶️  Выполнение: ramalama pull tinyllama

💾 Скачивание/загрузка модели...
   Файлы будут сохранены в: /workspace/models/
   Прогресс отображается в логах выше
```


## 📂 Структура директорий для хранения

### Автоматическое создание директорий

При установке и запуске проекта автоматически создается следующая структура:

```
ramalama-project/
├── models/                   # 🗂️ Скачанные модели (.gguf, .bin файлы)
│   ├── llama3.2:1b/         # Каждая модель в отдельной директории
│   └── tinyllama/           # Автоматически создается при скачивании
├── data/                    # 📊 Рабочие данные и логи
│   └── logs/                # 📋 Логи работы системы
└── backups/                 # 💾 Резервные копии моделей
    ├── ramalama_backup_*/   # Автоматически архивированные бэкапы
    └── *.tar.gz            # Сжатые архивы для экономии места
```

### Docker Volumes Mapping

| Хост (./) | Контейнер (/workspace/) | Назначение |
|-----------|-------------------------|------------|
| `models/` | `models/` | Хранение скачанных моделей |
| `data/` | `data/` | Рабочие данные и логи |

### Управление директориями

```bash
# Проверить и создать директории
./setup-dirs.sh

# Автоматически создаются при установке
./install.sh

# Вручную создать
mkdir -p models data backups data/logs

# Проверить содержимое
ls -la models/ data/
```

### Важные замечания

1. **Модели сохраняются на хосте** - файлы не теряются при перезапуске контейнера
2. **Относительные пути** - директории создаются относительно docker-compose.yml
3. **Права доступа** - автоматически устанавливаются при установке
4. **Мониторинг места** - регулярно проверяйте свободное место в models/

