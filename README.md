# RamaLama Docker Project

🚀 Полнофункциональное решение для запуска RamaLama через Docker с поддержкой proxy, мониторингом и автоматизацией.

## 🎯 Особенности

- ✅ **Полная поддержка HTTP/HTTPS proxy** - работает из коробки
- ✅ **Решена проблема зацикливания прокси** - нет бесконечных логов
- ✅ **Автоматическая установка** - один скрипт для настройки всего
- ✅ **Гибкое управление** - скрипты для всех операций
- ✅ **Мониторинг в реальном времени** - отслеживание состояния системы
- ✅ **Система бэкапов** - безопасное хранение моделей
- ✅ **Примеры использования** - готовые сценарии работы
- ✅ **Подробная документация** - решение всех проблем

## 📁 Структура проекта

```
ramalama-project/
├── Dockerfile                 # Определение Docker образа
├── docker-compose.yml        # Конфигурация Docker Compose
├── entrypoint.sh             # Точка входа с настройкой прокси
├── install.sh                # 🆕 Автоматическая установка
├── ramalama.sh               # Главный скрипт управления
├── quick-test.sh             # Быстрое тестирование
├── examples.sh               # 🆕 Примеры использования
├── monitor.sh                # 🆕 Мониторинг системы
├── backup.sh                 # 🆕 Бэкапы моделей
├── Makefile                  # 🆕 Команды Make
├── .env.example              # 🆕 Пример конфигурации
├── .gitignore                # Git игнорирование
├── README.md                 # Эта документация
├── TROUBLESHOOTING.md        # 🆕 Решение проблем
├── models/                   # Хранилище моделей (авто)
├── data/                     # Данные (авто)
│   └── logs/                 # Логи (авто)
└── backups/                  # Бэкапы (авто)
```

## 🚀 Быстрый старт

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# Один скрипт для всего!
chmod +x install.sh
./install.sh

# Установка автоматически:
# ✓ Проверит системные требования
# ✓ Настроит прокси (если нужен)
# ✓ Создаст конфигурацию
# ✓ Соберет Docker образ
# ✓ Запустит тесты
```

### Вариант 2: Ручная установка

```bash
# 1. Сделать скрипты исполняемыми
chmod +x *.sh

# 2. Собрать образ
./ramalama.sh build

# 3. Запустить тесты
./quick-test.sh

# 4. Проверить работу
./ramalama.sh version
./ramalama.sh info
```

## 🛠️ Дополнительные утилиты

### Мониторинг системы

```bash
# Интерактивный мониторинг
./monitor.sh

# Одноразовый снимок
./monitor.sh --snapshot

# JSON вывод для автоматизации
./monitor.sh --json

# С кастомным интервалом
./monitor.sh --interval 10
```

### Бэкапы

```bash
# Создать бэкап
./backup.sh create

# Список бэкапов
./backup.sh list

# Восстановить из бэкапа
./backup.sh restore backups/ramalama_backup_*.tar.gz

# Очистить старые бэкапы (оставить последние 3)
./backup.sh cleanup 3
```

### Примеры использования

```bash
# Показать все примеры
./examples.sh

# Примеры выполняют демонстрацию:
# - Базовых операций
# - Работы с моделями
# - Интерактивного чата
# - Режима сервера
# - Автоматизации
```

## 📖 Использование

### Управление моделями

```bash
# Скачать модель
./ramalama.sh pull llama3.2:1b

# Список скачанных моделей
./ramalama.sh list

# Удалить модель
./ramalama.sh rm llama3.2:1b
```

### Запуск моделей

```bash
# Интерактивный режим
./ramalama.sh run llama3.2:1b

# Запуск как сервер
./ramalama.sh serve llama3.2:1b --port 8080

# С дополнительными параметрами
./ramalama.sh run llama3.2:1b --temperature 0.7
```

### Дополнительные команды

```bash
# Открыть bash в контейнере
./ramalama.sh shell

# Пересобрать образ с нуля
./ramalama.sh rebuild

# Очистить контейнеры и образы
./ramalama.sh clean

# Прямой доступ к ramalama
./ramalama.sh -- <любая команда>
```

## 🔧 Настройка прокси

### Автоматическая настройка (рекомендуется)

Прокси автоматически подхватывается из переменных окружения хоста:

```bash
export HTTP_PROXY=http://127.0.0.1:2080
export HTTPS_PROXY=http://127.0.0.1:2080
```

### Ручная настройка

Отредактируйте `docker-compose.yml`:

```yaml
environment:
  - HTTP_PROXY=http://your-proxy:port
  - HTTPS_PROXY=http://your-proxy:port
```

### Работа без прокси

```bash
# Использовать профиль без прокси
docker-compose --profile no-proxy run ramalama-no-proxy <команда>
```

## 🔍 Решение проблем

### Проблема: Зацикливание на логах прокси

**Решение:** Используется упрощенная конфигурация прокси (только HTTP/HTTPS) и отключено избыточное логирование через `RAMALAMA_LOG_LEVEL=ERROR`.

### Проблема: Прокси недоступен из контейнера

**Решение:** Используется `network_mode: host` для прямого доступа к прокси на 127.0.0.1.

### Проблема: Модели не сохраняются

**Решение:** Проверьте примонтированные volumes:

```bash
ls -la models/
```

### Проблема: Медленная загрузка моделей

**Решение:** Проверьте работу прокси:

```bash
# Внутри контейнера
./ramalama.sh shell
curl -I https://huggingface.co
```

## 📊 Переменные окружения

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `HTTP_PROXY` | HTTP прокси | - |
| `HTTPS_PROXY` | HTTPS прокси | - |
| `NO_PROXY` | Исключения для прокси | localhost,127.0.0.0/8,::1 |
| `RAMALAMA_MODELS_PATH` | Путь к моделям | /workspace/models |
| `RAMALAMA_LOG_LEVEL` | Уровень логирования | ERROR |

## 🛠️ Продвинутое использование

### Работа с несколькими моделями

```bash
# Скачать несколько моделей
./ramalama.sh pull llama3.2:1b
./ramalama.sh pull phi3:mini

# Запустить разные модели
./ramalama.sh run llama3.2:1b
./ramalama.sh run phi3:mini
```

### Настройка памяти

Отредактируйте `docker-compose.yml`:

```yaml
deploy:
  resources:
    limits:
      memory: 8G
    reservations:
      memory: 4G
```

### Использование GPU

Добавьте в `docker-compose.yml`:

```yaml
deploy:
  resources:
    reservations:
      devices:
        - driver: nvidia
          count: 1
          capabilities: [gpu]
```

## 📝 Примеры работы

### Пример 1: Простой чат

```bash
./ramalama.sh pull llama3.2:1b
./ramalama.sh run llama3.2:1b
# Введите ваш запрос...
```

### Пример 2: API сервер

```bash
./ramalama.sh serve llama3.2:1b --port 8080

# В другом терминале:
curl http://localhost:8080/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"messages": [{"role": "user", "content": "Hello!"}]}'
```

### Пример 3: Скрипт обработки

```bash
echo "What is the capital of France?" | \
./ramalama.sh run llama3.2:1b --no-interactive
```

## 🔐 Безопасность

- Прокси-креденшиалы хранятся в переменных окружения
- Модели хранятся в изолированном volume
- Контейнер запускается без привилегий

## 📞 Поддержка

- Проверка версии: `./ramalama.sh version`
- Информация о системе: `./ramalama.sh info`
- Справка: `./ramalama.sh help`

## 📚 Документация

### Файлы документации

- **README.md** - Основная документация (этот файл)
- **TROUBLESHOOTING.md** - Подробное руководство по решению проблем
- **examples.sh** - Практические примеры использования
- **.env.example** - Пример конфигурационного файла

### Встроенная справка

```bash
# Справка по ramalama.sh
./ramalama.sh help

# Справка по monitor.sh
./monitor.sh --help

# Справка по backup.sh
./backup.sh --help

# Команды Make
make help
```

### Получение помощи

Если возникли проблемы:

1. Проверьте **TROUBLESHOOTING.md** - там решения 90% проблем
2. Запустите диагностику: `./monitor.sh --snapshot`
3. Проверьте логи: `docker-compose logs`
4. Запустите тесты: `./quick-test.sh`

## 🎓 Советы и лучшие практики

### Для начинающих

1. Начните с маленькой модели: `./ramalama.sh pull tinyllama`
2. Используйте автоматическую установку: `./install.sh`
3. Изучите примеры: `./examples.sh`
4. Мониторьте систему: `./monitor.sh`

### Для продвинутых

1. Настройте `.env` файл под свои нужды
2. Используйте `make` для быстрого доступа к командам
3. Автоматизируйте бэкапы через cron
4. Используйте мониторинг в JSON для интеграции

### Производительность

1. Используйте квантизированные модели (Q4/Q5) для экономии памяти
2. Ограничьте context-size если не нужен большой контекст
3. Настройте swap для больших моделей
4. Используйте GPU если доступен

## 🤝 Участие в разработке

Проект открыт для улучшений! Присылайте pull requests с:

- Исправлениями багов
- Новыми утилитами
- Улучшениями документации
- Оптимизацией производительности

## 📄 Лицензия

Проект использует RamaLama, который распространяется под лицензией Apache 2.0.

---

**Создано с ❤️ для упрощения работы с RamaLama**

Последнее обновление: Декабрь 2024
