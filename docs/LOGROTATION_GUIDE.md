# 📋 Руководство по ротации логов RamaLama

## 🎯 Обзор системы ротации логов

Система ротации логов RamaLama состоит из трех компонентов:

1. **Logrotate** - системный инструмент для автоматической ротации
2. **Log-manager.sh** - скрипт для ручного управления логами  
3. **Cron** - планировщик задач для автоматического запуска

## 📁 Структура логов

```
./logs/
├── ramalama.log              # Основной лог системы
├── monitor.log               # Лог мониторинга  
└── sessions/                 # Директория сессий
    ├── ramalama_session_*.log    # Логи отдельных сессий
```

## ⚙️ Настройка автоматической ротации

### Шаг 1: Запуск скрипта настройки

```bash
# Сделать скрипт исполняемым
chmod +x setup-logrotate.sh

# Запустить настройку
sudo ./setup-logrotate.sh
```

### Что делает скрипт:

1. **Создает централизованное логирование** в `/var/log/ramalama/`
2. **Настраивает символические ссылки** для интеграции с системой
3. **Устанавливает конфигурацию logrotate** в `/etc/logrotate.d/ramalama`
4. **Создает cron задачи** для автоматического запуска
5. **Перезапускает системные службы**

## 🔧 Ручное управление логами

### Использование log-manager.sh

```bash
# Инициализация системы логирования
./log-manager.sh init

# Просмотр последних 100 строк
./log-manager.sh show

# Просмотр последних 50 строк
./log-manager.sh show 50

# Мониторинг логов в реальном времени
./log-manager.sh tail

# Показать все сессии
./log-manager.sh sessions

# Очистка старых логов
./log-manager.sh clean

# Статус логирования
./log-manager.sh status

# Запуск команды с логированием сессии
./log-manager.sh run <команда>
```

### Ручной запуск logrotate

```bash
# Проверка конфигурации (без выполнения)
logrotate -d config/logrotate-ramalama.conf

# Принудительная ротация
logrotate -f config/logrotate-ramalama.conf

# Ротация с подробным выводом
logrotate -v config/logrotate-ramalama.conf
```

## ⏰ Автоматическая ротация

### Настройка через Cron

Cron задачи настраиваются автоматически при запуске `setup-logrotate.sh`:

```bash
# Просмотр cron задач
sudo crontab -l | grep ramalama

# Редактирование cron задач
sudo crontab -e
```

### Стандартные задачи:

- **Ежедневная очистка**: 02:30 (через log-manager.sh clean)
- **Еженедельная проверка**: Воскресенье 03:00 (проверка logrotate)

### Системная ротация logrotate

Logrotate автоматически запускается через `/etc/cron.daily/logrotate`:
- **Время**: Обычно в 06:25 утра
- **Конфигурация**: `/etc/logrotate.conf`
- **Дополнительные конфиги**: `/etc/logrotate.d/`

## 📊 Параметры ротации

### Настройки в `config/logrotate-ramalama.conf`:

- **Частота**: Ежедневно (`daily`)
- **Хранение**: 30 архивных копий (`rotate 30`)
- **Сжатие**: Автоматическое (`compress`)
- **Создание новых файлов**: Включено (`create`)
- **Безопасность**: 
  - `missingok` - игнорирует отсутствующие файлы
  - `notifempty` - не ротирует пустые файлы
- **Post-rotate скрипты**: Логирование завершения ротации

## 🧪 Тестирование системы

### Проверка конфигурации logrotate

```bash
# Отладка (показывает что будет делать)
logrotate -d config/logrotate-ramalama.conf

# Тестовый запуск (не выполняет ротацию)
logrotate --debug config/logrotate-ramalama.conf

# Проверка системной конфигурации
sudo logrotate -d /etc/logrotate.d/ramalama
```

### Проверка cron задач

```bash
# Статус cron daemon
sudo systemctl status cron

# Список активных задач
sudo crontab -l

# Логи cron
sudo tail -f /var/log/cron.log
```

### Создание тестового файла для ротации

```bash
# Создать тестовый файл
echo "Test log entry $(date)" > logs/test.log

# Настроить временную конфигурацию для теста
cat > /tmp/test-logrotate.conf << 'EOF'
logs/test.log {
    daily
    rotate 2
    compress
    missingok
    notifempty
}
EOF

# Запустить тестовую ротацию
logrotate -f /tmp/test-logrotate.conf

# Проверить результат
ls -la logs/test.log*
```

## 🔍 Мониторинг ротации

### Просмотр логов ротации

```bash
# Основной лог системы
tail -f logs/ramalama.log

# Лог мониторинга
tail -f logs/monitor.log

# Системные логи cron
sudo tail -f /var/log/cron.log

# Логи logrotate (если настроены)
sudo tail -f /var/log/syslog | grep logrotate
```

### Проверка состояния файлов

```bash
# Размеры лог-файлов
du -sh logs/*

# Количество файлов в каждой категории
find logs/ -name "*.log" | wc -l
find logs/sessions/ -name "*.log" | wc -l

# Самые старые файлы
find logs/ -name "*.log" -type f -printf '%T@ %p\n' | sort -n | head -5
```

## 🛠️ Устранение неполадок

### Проблема: Логи не ротируются

```bash
# Проверить права доступа
ls -la logs/

# Проверить конфигурацию
logrotate -d config/logrotate-ramalama.conf

# Принудительная ротация
logrotate -f config/logrotate-ramalama.conf
```

### Проблема: Cron задачи не выполняются

```bash
# Проверить статус cron
sudo systemctl status cron

# Проверить синтаксис crontab
sudo crontab -l

# Проверить системные логи
sudo journalctl -u cron
```

### Проблема: Недостаток места

```bash
# Найти самые большие файлы
find logs/ -type f -exec du -h {} + | sort -rh | head -10

# Очистить старые архивы вручную
find logs/ -name "*.gz" -mtime +30 -delete

# Использовать log-manager.sh clean
./log-manager.sh clean
```

## 📋 Команды быстрого доступа

```bash
# Проверить статус системы логирования
./log-manager.sh status

# Очистить старые логи
./log-manager.sh clean

# Принудительная ротация через logrotate
logrotate -f config/logrotate-ramalama.conf

# Мониторинг логов в реальном времени
./log-manager.sh tail

# Проверка конфигурации logrotate
logrotate -d config/logrotate-ramalama.conf

# Статус cron и задач
sudo systemctl status cron && sudo crontab -l | grep ramalama
```

## 🎯 Результат

После настройки система ротации логов будет работать автоматически:

- **Ежедневно**: Ротация логов через системный logrotate
- **Ежедневно в 02:30**: Дополнительная очистка через cron
- **Еженедельно**: Проверка состояния системы
- **По требованию**: Ручная очистка и ротация

Все логи будут автоматически:
- Архивироваться с timestamp
- Сжиматься для экономии места  
- Храниться в течение 30 дней
- Удаляться при превышении лимита
---

## 🌐 Translations / Переводы

| Язык | Документация | Статус перевода |
|------|--------------|-----------------|
| 🇺🇸 English | [LOGROTATION_GUIDE.en.md](LOGROTATION_GUIDE.en.md) | ✅ Translation |
| 🇷🇺 Russian | [LOGROTATION_GUIDE.md](LOGROTATION_GUIDE.md) | ✅ Оригинал |
| 🇺🇸 English | [README.md](../README.md) | ✅ Translation |
| 🇷🇺 Russian | [README.ru.md](../logs/README.ru.md) | ✅ Оригинал |
