# 🔧 Срочные исправления

## Обнаруженные проблемы и решения

### ❌ Проблема 1: Логи прокси повторяются

**Симптомы:**
```
INFO:ramalama:Using proxy: https=http://127.0.0.1:2080
INFO:ramalama:Using proxy: http=http://127.0.0.1:2080
[повторяется многократно]
```

**Решение:** Обновлен `entrypoint.sh` с фильтрацией логов через grep.

---

### ❌ Проблема 2: Команда `version` не работает

**Симптомы:**
```
ramalama: error: unrecognized arguments: --version
```

**Причина:** RamaLama использует подкоманду `version`, а не флаг `--version`.

**Решение:** Обновлены `ramalama.sh` и `quick-test.sh`.

---

### ❌ Проблема 3: RamaLama запускается в --nocontainer режиме

**Симптомы:**
```
Error: RamaLama requires the "llama-server" command to be installed on the host 
when running with --nocontainer.
```

**Причина:** RamaLama определяет окружение и пытается запуститься на хосте вместо контейнера.

**Решение:** Добавлена переменная `RAMALAMA_IN_CONTAINER=1` в `docker-compose.yml`.

---

### ❌ Проблема 4: Ошибка проверки памяти в install.sh

**Симптомы:**
```
./install.sh: строка 119: [: -gt: ожидается унарный оператор
```

**Решение:** Добавлена проверка на пустую переменную в `install.sh`.

---

## 📦 Применение исправлений

### Вариант 1: Быстрое обновление (рекомендуется)

```bash
# 1. Пересоберите образ с новым entrypoint.sh
./ramalama.sh rebuild

# 2. Проверьте работу
./ramalama.sh -- version
./ramalama.sh info
```

### Вариант 2: Полная переустановка

```bash
# 1. Остановите контейнеры
docker-compose down -v

# 2. Пересоберите образ
docker-compose build --no-cache

# 3. Проверьте
./ramalama.sh -- version
```

### Вариант 3: Ручное обновление файлов

Обновите следующие файлы из артефактов:

1. **entrypoint.sh** - добавлен grep фильтр для логов
2. **docker-compose.yml** - добавлена переменная RAMALAMA_IN_CONTAINER
3. **ramalama.sh** - исправлена команда version
4. **quick-test.sh** - исправлен тест version
5. **install.sh** - исправлена проверка памяти

Затем:
```bash
chmod +x entrypoint.sh
./ramalama.sh rebuild
```

---

## ✅ Проверка исправлений

После применения исправлений выполните:

```bash
# 1. Проверка version
./ramalama.sh -- version
# Должно вывести JSON с версией, БЕЗ повторяющихся логов прокси

# 2. Проверка info
./ramalama.sh info
# Должно вывести информацию о системе

# 3. Проверка pull
./ramalama.sh pull tinyllama
# Должно скачать модель

# 4. Проверка run (важный тест!)
./ramalama.sh run tinyllama
# Должно запустить интерактивный чат БЕЗ ошибки про llama-server
```

---

## 🔍 Если проблемы остались

### Логи прокси все еще появляются?

```bash
# Добавьте в .env
echo "PYTHONWARNINGS=ignore" >> .env

# Пересоберите
./ramalama.sh rebuild
```

### RamaLama все еще ищет llama-server на хосте?

Проверьте что в `docker-compose.yml` есть:
```yaml
environment:
  - RAMALAMA_IN_CONTAINER=1
```

Если нет - добавьте вручную и пересоберите:
```bash
./ramalama.sh rebuild
```

### Команда version все еще не работает?

Используйте альтернативный синтаксис:
```bash
# Вместо
./ramalama.sh version

# Используйте
./ramalama.sh -- version
```

---

## 📊 Ожидаемый результат

После всех исправлений вывод должен выглядеть так:

```bash
$ ./ramalama.sh -- version
✓ Директории models и data готовы
Creating ramalama-project_ramalama_run ... done
✓ Proxy configured: http://127.0.0.1:2080
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  RamaLama Docker Environment
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Models path: /workspace/models
Proxy: http://127.0.0.1:2080
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
0.15.0

$ ./ramalama.sh run tinyllama
[модель запускается и работает без ошибок про llama-server]
```

**БЕЗ** повторяющихся строк `INFO:ramalama:Using proxy`.

---

## 🆘 Дополнительная помощь

Если проблемы не решены:

1. Проверьте версии файлов (должна быть дата обновления)
2. Запустите полную диагностику: `./monitor.sh --snapshot`
3. Проверьте логи: `docker-compose logs | grep -i error`
4. См. TROUBLESHOOTING.md для детальной диагностики

---

**Дата исправления:** 12 декабря 2024
